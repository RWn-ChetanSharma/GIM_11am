#include<stdio.h>

/*
	Variables :- A container which stores some data is known as Variable.
	
		Rules for declaring a variable :-
			1. The first letter must be an alphabetic letter or _
			2. Variable name can’t contain a whitespace
			3. A number or any special character cannot be placed in the first position
			4. Variable name can’t be same as keyword
*/


main(){
	
//	keyword  variable = data Type;
	int a = 10;
	
	printf("%d", a);
	
//	%d => Format Specifier
}
